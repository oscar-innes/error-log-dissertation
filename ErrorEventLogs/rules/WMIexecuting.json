{
    "rule_title": "TTP 1546.003 - WMI Consumer Executing Shell Commands",
    "description": "Detects WMI CommandLine consumers configured to execute cmd.exe, PowerShell, or other interpreters — the payload execution half of a WMI subscription. The Destination field contains the command that will run each time the filter triggers.",
    "logic": "AND",
    "severity": "critical",
    "detection": [
      { "field": "EventID", "op": "eq", "value": "20" },
      { "field": "Channel", "op": "eq", "value": "Microsoft-Windows-Sysmon/Operational" },
      { "field": "ED_Type", "op": "eq", "value": "Command Line" },
      { "field": "ED_Destination", "op": "contains_any", "value": [
          "cmd.exe",
          "powershell",
          "wscript",
          "cscript",
          "mshta",
          "rundll32"
        ]
      }
    ]
  }