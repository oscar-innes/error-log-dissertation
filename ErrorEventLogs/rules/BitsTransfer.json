 {
    "rule_title": "TTP 1197 - BITS Transfer Command Line",
    "description": "Detects BITS download jobs initiated via bitsadmin.exe with the /transfer flag. The command line exposes the job name, remote URL, and local destination path. bitsadmin is rarely used legitimately in modern environments — any /transfer invocation pointing to an external URL is high fidelity.",
    "logic": "AND",
    "severity": "high",
    "detection": [
      { "field": "EventID", "op": "in", "value": ["4104", "800", "4688"] },
      { "field": "Channel", "op": "in", "value": ["Microsoft-Windows-PowerShell/Operational", "Windows PowerShell", "Security"] },
      { "field": "ED_NewProcessName", "op": "endswith", "value": "\\bitsadmin.exe" },
      { "field": "ED_CommandLine", "op": "in", "value": ["/transfer", "Start-BitsTransfer"] },
      { "field": "ED_CommandLine", "op": "contains_any", "value": ["http://", "https://", "ftp://"] }
    ]
  }